version: "3.1"
session_config:
  session_expiration_time: 0
  carry_over_slots_to_new_session: true
intents:
  - poggers

responses:
  utter_pog:
    - text: That's right! Poggers

actions:
  - my_mom_action